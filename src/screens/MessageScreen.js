import React from "react";
import { Text, View } from "react-native";

export default MessageScreen = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Mes Screen</Text>
    </View>
  );
};
